version: '3.8'

services:
  diagram-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.gpu
    environment:
      - DEVICE=cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
